# === Windows ===
Thumbs.db
ehthumbs.db
ehthumbs_vista.db
Desktop.ini
$RECYCLE.BIN/
*.lnk

# === PowerShell ===
*.ps1xml.cache
PSReadLine_history.txt

# === Python ===
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
*.egg
.venv/
venv/
ENV/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# === Node.js ===
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.npm
.yarn/
.pnp.*

# === Editor / IDE ===
.vscode/
.idea/
*.swp
*.swo
*~

# === Logs (cartella tracciata, contenuto ignorato) ===
logs/*
!logs/.gitkeep

# === Backup config sensibili (cartella tracciata, contenuto ignorato) ===
backup/*
!backup/.gitkeep
*.reg

# === Env files ===
.env
.env.*
!.env.example

# === Claude Code ===
.claude/*
!.claude/agents/
!.claude/agents/**
!.claude/settings.json
.claude/worktrees/
.claude/scheduled_tasks.lock
# Note: .claude/settings.local.json resta gitignored (personal overrides via pattern .claude/*)
# Note: .claude/.session-start-head e' gitignored (marker file H12 hook)
.aider*

# === Quality bench results (framework tracked, run outputs ignored) ===
scripts/quality-bench/results/
